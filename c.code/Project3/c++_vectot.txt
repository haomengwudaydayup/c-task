#include<cstdio>
#include<iostream>
#include<vector>
#include<array>
#include<cstdlib>

using namespace std;

int main()
{
	vector<double> a1 = { 1.23,23.4,11.4,9.8 };
	/*a1容器的长度为*/
	cout << a1.size() << endl;
	/*创建迭代对象*/
	vector<double>::iterator it;
	/*返回第一元素的值*/
	cout << a1.front() << endl;
	/*访问第3个元素的值*/
	a1[2] = 99;
	cout << a1[2] << endl;
	/*采用at()进行索引*/
	a1.at(2) = 77;
	cout << a1.at(2) << endl;//使用at()可以进行索引，但是运行时间长


	system("pause");
	return 0;
}